var MapStart =
{
    gameStart: function(map)
    {
        // configure the map setup for your specific mod here.
		// this function gets called once for each mod right before the map gets started
    },
}
